-- ============================================================
-- GeminiGen 视频生成充值方案 - 数据库配置脚本
-- 执行前请备份数据库！
-- ============================================================

-- ------------------------------------------------------------
-- 第一步：创建用户分组（通过 group_ratio 配置）
-- 此配置在「系统设置 - 分组与模型倍率设置」中也可手动配置
-- 以下 SQL 直接写入 Option 表（如使用 SQLite/MySQL/PostgreSQL 请调整语法）
-- ------------------------------------------------------------

-- 分组倍率配置（group_ratio）
-- default: 2.0x 视频, 1.5x 图片, 1.0x 对话
-- video_creator: 1.0x 视频, 1.2x 图片, 1.0x 对话
-- video_pro: 0.8x 视频, 1.0x 图片, 1.0x 对话
-- enterprise: 0.6x 视频, 0.8x 图片, 1.0x 对话

-- 注意：group_ratio 在 Option 表中存储为 JSON
-- 请先在后台「系统设置 - 分组与模型倍率设置」中添加以下分组：
--   video_creator, video_pro, enterprise
-- 并设置对应倍率（详见下方配置指南）

-- ------------------------------------------------------------
-- 第二步：创建订阅套餐（subscription_plans 表）
-- ------------------------------------------------------------

INSERT INTO subscription_plans (
    title, subtitle, price_amount, currency,
    duration_unit, duration_value, custom_seconds,
    enabled, sort_order,
    stripe_price_id, creem_product_id,
    max_purchase_per_user,
    upgrade_group,
    total_amount,
    quota_reset_period, quota_reset_custom_seconds
) VALUES
(
    '免费体验',
    '每日 500 额度，体验 Veo 视频生成',
    0,
    'USD',
    'month', 1, 0,
    1, 1,
    '', '',
    1,  -- 每人限领1次
    '',
    500,
    'daily', 0
),
(
    '视频创作者',
    '每月 30,000 额度，视频模型成本价',
    29,
    'USD',
    'month', 1, 0,
    1, 2,
    '', '',
    0,  -- 不限购买次数
    'video_creator',
    30000,
    'monthly', 0
),
(
    '视频专业版',
    '每月 100,000 额度，视频+图片成本价，优先队列',
    69,
    'USD',
    'month', 1, 0,
    1, 3,
    '', '',
    0,
    'video_pro',
    100000,
    'monthly', 0
),
(
    '企业API',
    '每月 500,000 额度，全模型最低倍率，高并发',
    199,
    'USD',
    'month', 1, 0,
    1, 4,
    '', '',
    0,
    'enterprise',
    500000,
    'monthly', 0
);

-- ------------------------------------------------------------
-- 第三步：配置支付金额选项（payment_setting）
-- 在后台「系统设置 - 支付设置」中配置
-- 建议预设金额：10, 29, 69, 100, 199, 500
-- ------------------------------------------------------------

-- 此配置通过后台 UI 完成：
-- 路径：管理员后台 → 系统设置 → 支付设置
-- 设置项：
--   - 充值金额选项: [10, 29, 69, 100, 199, 500]
--   - 最低充值金额: 1
--   - 金额折扣: {100: 0.95, 200: 0.9, 500: 0.85}

-- ------------------------------------------------------------
-- 配置完成后的效果
-- ------------------------------------------------------------

-- 免费体验：
--   - 用户可免费领取，每日获得 500 quota
--   - 约可生成 1 条 Veo Light (default 组 2x 倍率)
--   - 额度每日重置

-- 视频创作者 (¥29/月)：
--   - 购买后自动升级到 video_creator 分组
--   - 视频模型按 1x 成本价计费
--   - 30,000 quota ≈ 17 条 Veo Fast 或 120 条 Veo Light

-- 视频专业版 (¥69/月)：
--   - 购买后自动升级到 video_pro 分组
--   - 视频模型 0.8x 成本价，图片模型 1x 成本价
--   - 100,000 quota ≈ 71 条 Veo Fast 或 500 条 Veo Light

-- 企业API (¥199/月)：
--   - 购买后自动升级到 enterprise 分组
--   - 视频模型 0.6x 成本价，图片模型 0.8x 成本价
--   - 500,000 quota ≈ 476 条 Veo Fast 或 3333 条 Veo Light

-- 未订阅用户：
--   - 保留在 default 分组
--   - 视频模型 2x 倍率（相对较贵，鼓励订阅）
--   - 通过钱包充值按量付费
