- 2026-05-25: 实现提示词库媒体文件数据库存储方案（base64），新增 `prompt_media` 表，独立存储 cover_image/video 的 base64 数据。新增 `controller/prompt_media.go`（上传/获取/删除）和公开路由 `/api/public/prompt-media/:id`。前端 `EditPromptModal.jsx` 上传接口改为 `/api/prompt-media`（formData 字段 `file` + `media_type`）。Go 编译通过，已 push stable（commit `f61bb6bc`）。
- 2026-05-25: 文章系统图片同样改为数据库存储。新增 `article_media` 表，`service/article_image.go` 中 AI 生成图片后自动下载并入库，返回 `/api/public/article-media/:id` 本地 URL。`controller/article_media.go` 提供上传/获取/删除接口。前端 `ArticleManagement` 封面图增加上传按钮（Semi Upload 组件）。已 push stable（commit `8b64082c`）。
- 2026-05-25: 文章系统增加视频封面支持。`Article` 模型增加 `video_url` 和 `media_type` 字段，前端管理页面增加封面类型切换（图片/视频）和视频上传入口，文章详情页和画廊列表支持视频播放器展示。`article_media` 上传接口放宽限制支持视频文件（最大 100MB）。已 push stable（commit `97f0952f`）。
- 2026-05-25: 修复 React error #130 —— `Form.Subscribe` 在 `@douyinfe/semi-ui@2.69.1` 中不存在，导致提示词库和文章管理页面渲染崩溃。将 `Form.Subscribe` 替换为 `useState` + `Form.onValueChange` 实现条件渲染。已 push stable（commit `3eaaa866`）。
- 2026-05-25: 修复提示词库新增/编辑点击"提交"没反应的问题。根因：`content_en` 字段设置了 `required: true`，但默认显示中文标签页（`DEFAULT_LANG='zh'`），英文输入框被 `display: none` 隐藏。用户没填英文内容时表单校验失败，错误提示也看不到。修复：① `content`（中文）加 required；② `content_en` 去掉 required；③ 提交时如果 `content_en` 为空，自动用 `content` 填充。已 push stable（commit `b667ef8d`）。
- 2026-05-25: 视频上传时自动提取首帧作为封面图。前端新增 `extractVideoFirstFrame` 函数，利用 HTML5 Video + Canvas 从用户选择的视频文件中截取第 1 帧，转成 JPEG 后上传到媒体接口作为 `cover_image_url`。提示词库和文章系统均支持。首帧提取失败不影响视频上传本身。已 push stable（commit `cdc8c76e`）。
- 2026-05-25: 调研火山引擎视觉生成模型。结论：视频生成（Seedance）已通过 `ChannelTypeDoubaoVideo` 接入，支持 2.0/1.5/1.0 系列；图片生成（Seedream 4.0）已通过 `ChannelTypeVolcEngine` 接入。但缺少 Seedream 5.0 lite、4.5 和 Seedance 2.0 简短别名。已补充到模型列表（`volcengine/constants.go` + `doubao/constants.go`），Go 编译通过，已 push stable（commit `7511f35f`）。
- 2026-05-25: 排查 GeminiGen-VEO 渠道失败后未切换到章鱼哥AI-Veo 的问题。根因：`RetryTimes = 0`（默认不重试）。当设为 0 时，请求失败直接返回错误，不会尝试其他优先级渠道。解决：后台「运营设置」→「通用设置」→「失败重试次数」设为 1 或更大。
- 2026-05-25: 实现渠道模型自动熔断与恢复机制。新增 `setting/operation_setting/channel_health_setting.go` 配置（`auto_disable_consecutive_fails` + `auto_enable_minutes`）。扩展 `model/channel_health.go`：当某 (channel, model) 连续失败达到阈值时，临时禁用该组合（权重降为 0），请求自动打到其他渠道的相同模型；`DecayChannelHealth` 定时衰减时同步清除过期禁用标记。前端「运营设置」新增两个配置项。已 push stable（commit `434029b5`）。
