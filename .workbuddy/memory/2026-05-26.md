- 2026-05-26: 修复支付回调地址出现 `<nil>` 导致易支付跳转失败的问题。根因：数据库中 `ServerAddress` 或 `CustomCallbackAddress` 存储了字符串 `"<nil>"`。修复：`service/epay.go` 增加 `isInvalidAddress()` 过滤空字符串和 `"<nil>"`；`controller/topup.go` 中 `ServerAddress` 无效时从请求 Host 自动推断兜底地址。已 push stable（commit `5642482f`）。
- 2026-05-26: 修复任务轮询时未检查上游 HTTP 状态码导致任务状态不更新的问题。`updateVideoSingleTask` 中 `FetchTask` 后未检查 `resp.StatusCode`，当上游返回 404（任务不存在）或 HTML 错误页时，`ParseTaskResult` 解析失败返回 error，外层仅 LogError 不更新任务状态，应用端永远看不到失败。修复：4xx 直接标记任务为 FAILURE 并解析错误消息；5xx 返回 error 等待下一轮轮询。本地已提交（commit `fdf117fd`），push 因网络问题暂未完成。
