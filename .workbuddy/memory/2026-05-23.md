- 2026-05-23: 电商图向导菜单不显示问题排查 - 根因是 `SiderBar.jsx`、`App.jsx`、`router/api-router.go`、`model/main.go` 的修改未 commit 到 `98bd57d9`，已补 commit `9adb17f0` 并 push。
- 2026-05-23: 为用户编写电商图向导种子数据 SQL（模特呈现方式4条 + 案例品类4条 + 案例详情7条），含 Unsplash 对应图片和拍摄参数，文件 `docs/电商图向导_种子数据.sql`，已 push stable。
- 2026-05-23: 生成 8 张各平台风格 AI 商品图（淘宝/天猫/京东/Amazon/TikTok/小红书/Shopify/Temu/SHEIN），更新 v3 种子数据 SQL（24条案例详情），修复 `ecommerce_case_details` INSERT 列数不匹配问题（commit `09a9302d`）。图片通过 git 中转上传至服务器 `uploads/ecommerce/`。
- 2026-05-23: 修复图片 404 问题——Dockerfile `WORKDIR /data` 但 docker-compose.yml 挂载到 `/app/uploads` 路径不匹配，修正为 `/data/uploads`（commit `b790d944`）。
- 2026-05-23: 修复图片被定时清理问题——`service/upload_cleanup.go` 每日清理 uploads 目录，仅 `/permanent/` 白名单跳过。电商图向导图片在 `uploads/ecommerce/` 不在白名单，加入 `/ecommerce/` 跳过规则（commit `49a0adde`）。
- 2026-05-23: 重新设计电商图向导拍摄方式模型——`EcommerceCaseDetail` 增加 `pose_id` 字段，公开 API 查询改为 `?platform=xxx&pose=xxx`。model-poses 扩展为 8 种（白底/场景/穿戴/手持/平铺/细节/卖点/尺码），case-detail 种子数据 v4 含 32 条（8平台×4核心方式）。前端管理页面同步更新（commit `2bee670f`）。
